{{ form_start(form) }}
<div class="row">
    <p class="col-md-4"><strong>Auteur : {{ app.user.realName }}</strong></p>
    <label for="username" class="col-md-6">{{ form_row(form.author) }}</label>
</div>
<div class="row">
    <div class="col-md-12">{{ form_row(form.content) }}</div>
</div>
{{ form_widget(form) }}
<button class="btn btn-primary">{{ button|default('Enregistrer') }}</button>
{{ form_end(form) }}