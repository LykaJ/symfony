{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="login-form">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-primary">{{ button|default('S\'inscrire') }}</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}